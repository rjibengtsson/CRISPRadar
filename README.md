# CRISPRadar
CRISPRadar scans any input sequence to identify candidate guide RNAs matching a user-defined guide length and PAM sequence. It supports flexible PAM specification (including degenerate bases) and returns all matching candidate sites for downstream filtering or scoring.
